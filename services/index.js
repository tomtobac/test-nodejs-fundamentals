const images = require('../data/images.json');
const users = require('../data/users.json');

const getImageById = (imageId) => {
  // @todo: return an image given an image id
}

const getUserById = (userId) => {
  // @todo: return an user given an user id
}


module.exports = {
  getImageById,
  getUserById
}